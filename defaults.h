#ifndef DEFAULTS_H
#define DEFAULTS_H

#include <QString>

namespace Defaults {
    namespace MYSQL {
        const QString HOST = "localhost";
        const int PORT = 3306;
        const QString USER = "root";
        const QString PASSWORD = "root";
    }
}

#endif // DEFAULTS_H
